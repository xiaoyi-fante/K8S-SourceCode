# 第32章 k8s 存储对象源码解读

> **适用版本**: Kubernetes v1.21
> **对应章节**: 第 32 章 — k8s 存储对象源码解读
> **源码入口**: `pkg/controller/volume/persistentvolume/pv_controller_base.go → NewController:79`

---

## 核心机制一览

1. **PV/PVC 是资源与请求的分离**：PV 是集群级别的存储资源（由管理员预创建或动态 Provision），PVC 是 Pod 对存储资源的请求声明。二者通过 Bind 关联，生命周期遵循 `Provisioning → Binding → Using → Releasing → Recycling`。

2. **PersistentVolumeController 的双 Worker 架构**：控制器内部有两条独立的处理流水线——`volumeWorker` 处理 PV 的 add/update/delete，`claimWorker` 处理 PVC 的 add/update/delete。两条流水线分别消费各自的 workqueue，互不阻塞。

3. **syncVolume 的六动作状态机**：PV 的同步逻辑是一个多分支状态机。核心分支：① PV 未绑定（Available）→ 寻找匹配 PVC 并 bind；② PV 已绑定（Bound）→ 校验 PVC 是否存在并更新状态；③ PV 已释放（Released）→ 触发 `reclaimVolume`；④ PV 绑定到其他 PVC → 调 `unbindVolume` 重置。

4. **三种 ReclaimPolicy**：`Retain`（保留数据，仅记日志）、`Delete`（调插件 `deleter.Delete()` 删除后端存储，再删 etcd 中 PV 对象）、`Recycle`（起一个 busybox recycler pod 执行 `rm -rf /scrub/*`，完成后调 `unbindVolume` 将 PV 重置为 Available）。

5. **syncUnboundClaim 的两条动态/静态路径**：PVC 未绑定时，先调 `findBestMatchForClaim` 在现有 PV 中寻找满足 AccessMode、容量等条件的静态 PV；找不到且配置了 StorageClass 则调 `provisionClaim` 动态创建 PV，Provisioner 由插件体系或外部 external-provisioner 实现。

---

## 全章调用链总图

```
kube-controller-manager 启动
  │
  ▼ startPersistentVolumeBinderController（core.go:284）
  │  ProbeVolumePlugins（探测各存储类型插件）
  │  NewController(params)（pv_controller_base.go:79）
  │
  ▼ NewController
  │  ├─── VolumeInformer.AddEventHandler → enqueueWork(volumeQueue)
  │  ├─── ClaimInformer.AddEventHandler  → enqueueWork(claimQueue)
  │  ├─── ClassInformer / PodInformer / NodeInformer（只同步，不入队）
  │  └─── volumePluginMgr.InitPlugins    （plugins.go:594）
  │         遍历 allPlugins，调 plugin.Init()，塞入 plugins map
  │
  ▼ ctrl.Run（pv_controller_base.go:303）
  │  cache.WaitForNamedCacheSync（等待所有 Informer list 完成）
  │  ctrl.initializeCaches（用 Lister 数据填充 PV/PVC 本地缓存）
  │  go wait.Until(ctrl.resync,       resyncPeriod, stopCh)
  │  go wait.Until(ctrl.volumeWorker, time.Second,  stopCh)
  │  go wait.Until(ctrl.claimWorker,  time.Second,  stopCh)

volumeWorker（pv_controller_base.go:411）
  │  workFunc：消费 volumeQueue，判断动作类型
  │  ├─── add/update/sync → updateVolume → syncVolume（pv_controller.go:542）
  │  └─── delete          → deleteVolume

claimWorker（pv_controller_base.go:469）
  │  workFunc：消费 claimQueue，判断动作类型
  │  ├─── add/update/sync → updateClaim → syncClaim（pv_controller.go:252）
  │  └─── delete          → deleteClaim
```

---

## §01 k8s 存储管理发展历程和 volume 简介

### 存储管理的演进

Kubernetes 存储管理经历了三个阶段：

| 阶段 | 方式 | 问题 |
|------|------|------|
| In-tree volume | 存储驱动代码写在 k8s 源码中（如 `pkg/volume/nfs/`）| 每次新增存储类型都要修改 k8s 核心代码，发布周期慢 |
| FlexVolume | 插件以二进制形式放在节点上，kubelet 通过 exec 调用 | 需要 root 权限，安装复杂，调试困难 |
| CSI（Container Storage Interface）| 独立的存储插件进程，通过 gRPC 与 kubelet 交互 | 目前推荐方案，完全解耦 |

### volume 类型分类

```
Volume 类型
  │
  ├─── 本地存储
  │     ├─── emptyDir      容器共享临时目录（随 Pod 生灭）
  │     ├─── hostPath      挂载节点上的文件/目录（危险：节点耦合）
  │     └─── local         本地持久化（需要 PV，绑定到特定节点）
  │
  ├─── 网络存储（NFS、GlusterFS、Ceph 等）
  │
  ├─── 云存储（AWS EBS、Azure Disk、GCE PD 等）
  │
  ├─── ConfigMap / Secret  配置数据挂载为文件
  │
  └─── PersistentVolumeClaim（PVC）
        ↑ 推荐方式：通过 PVC 解耦 Pod 与具体存储类型
```

### 为什么需要 PV/PVC

- **解耦关注点**：Pod 开发者只需声明"需要多大容量、什么 AccessMode"（PVC），不需要关心底层是 NFS 还是 Ceph。
- **生命周期独立**：PV 的生命周期独立于 Pod，Pod 删除后存储数据仍在。
- **资源复用**：静态 PV 可以被多个 PVC 依次使用（经回收后）。

---

## §02 PV 和 PVC 简介、静态 PV 和动态 PV 示例

### PV 核心字段

```yaml
apiVersion: v1
kind: PersistentVolume
metadata:
  name: pv-nfs-001
spec:
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteMany        # RWO / ROX / RWX
  persistentVolumeReclaimPolicy: Retain   # Retain / Delete / Recycle
  nfs:
    server: 192.168.1.100
    path: /data/pv001
```

**accessModes 三种模式**：

| 模式 | 缩写 | 含义 |
|------|------|------|
| ReadWriteOnce | RWO | 单节点读写 |
| ReadOnlyMany  | ROX | 多节点只读 |
| ReadWriteMany | RWX | 多节点读写 |

### PVC 核心字段

```yaml
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: my-pvc
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 500Mi
  storageClassName: ""     # 空字符串 = 不使用 StorageClass，匹配静态 PV
```

PVC 绑定 PV 的条件（同时满足）：
- accessModes 兼容
- 容量满足（PV 容量 ≥ PVC 请求）
- storageClassName 匹配
- selector（label）匹配（可选）

### PV/PVC 生命周期

```
Provisioning（创建 PV）
  │
  ▼ Binding（PV 与 PVC 相互绑定）
  │
  ▼ Using（Pod 使用 PVC）
  │
  ▼ Releasing（PVC 删除，PV 进入 Released 状态）
  │
  ▼ Recycling（按 ReclaimPolicy 处理）
       ├─── Retain  → PV 保留（Available=false，需管理员手动处理）
       ├─── Delete  → 删除后端存储 + PV 对象
       └─── Recycle → 清空数据，PV 重回 Available
```

**PV/PVC 关系示意**：

```
Pod
 │  引用
 ▼
PVC ──── Check ──→ StorageClass
 │                      │
 │ Bind           Provisioner（动态创建）
 ▼                      │
PV ←──── Provision ─────┘
```

### 静态 PV 示例（NFS）

```bash
# 1. 创建 NFS PV（管理员操作）
kubectl create -f pv-nfs.yaml

# 2. 创建 PVC（开发者操作）
kubectl create -f pvc.yaml

# 3. PVC 状态变为 Bound，查看绑定详情
kubectl get pvc
# NAME    STATUS  VOLUME      CAPACITY  ACCESS MODES
# my-pvc  Bound   pv-nfs-001  1Gi       RWX

# 4. Pod 引用 PVC
volumes:
  - name: data
    persistentVolumeClaim:
      claimName: my-pvc
```

### 动态 PV 示例（NFS Provisioner）

动态 PV 通过 StorageClass + 外部 Provisioner 自动创建：

```yaml
# StorageClass 定义
apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: managed-nfs-storage
provisioner: fuseim.pri/ifs           # 对应外部 provisioner 的名字
parameters:
  archiveOnDelete: "false"
```

外部 Provisioner（`nfs-subdir-external-provisioner`）部署为 Deployment，持续监听 PVC 事件；当 PVC 的 `storageClassName` 匹配时，自动在 NFS 上创建子目录并绑定 PV。

动态 PV 命名格式：`{namespace}-{pvcName}-{pvName}`，挂载路径格式：`/archived-{namespace}-{pvcName}-{pvName}`（archiveOnDelete=true 时）。

测试流程：
```bash
# 创建 PVC 和 Pod
kubectl create -f deploy/test-claim.yaml -f deploy/test-pod.yaml

# 验证动态 PV 被创建
kubectl get pvc test-claim    # STATUS: Bound

# 验证 NFS 上目录被创建
ls /data/nfs/default/SUCCESS  # provisioner 写入的标志文件

# 删除后目录归档（不删除数据）
kubectl delete -f deploy/test-pod.yaml deploy/test-claim.yaml
```

---

## §03 PV 控制器源码解读之控制器初始化

| 读码目标 | 源文件（可点击） | 入口函数 |
|---------|----------------|---------|
| 控制器注册入口 | [core.go](kubernetes/cmd/kube-controller-manager/app/core.go) | `startPersistentVolumeBinderController:284` |
| 控制器构造 | [pv_controller_base.go](kubernetes/pkg/controller/volume/persistentvolume/pv_controller_base.go) | `NewController:79` |
| 插件初始化 | [plugins.go](kubernetes/pkg/volume/plugins.go) | `InitPlugins:594` |
| 控制器主循环 | [pv_controller_base.go](kubernetes/pkg/controller/volume/persistentvolume/pv_controller_base.go) | `Run:303` |

### startPersistentVolumeBinderController

在 kube-controller-manager 的 `core.go:284` 中，`startPersistentVolumeBinderController` 负责：

1. 探测所有存储插件（`ProbeVolumePlugins`）——包括 in-tree 插件（NFS/HostPath/EmptyDir 等）、FlexVolume 插件、CSI 迁移插件
2. 构造 `ControllerParameters`，注入各类 Informer（Volume/Claim/Class/Pod/Node）
3. 调用 `NewController(params)` 实例化控制器

```go
// cmd/kube-controller-manager/app/core.go:284
func startPersistentVolumeBinderController(ctx ControllerContext) (http.Handler, bool, error) {
    // ...
    plugins, err := ProbeVolumePlugins(ctx.ComponentConfig.PersistentVolumeBinderController.VolumeConfiguration)
    // ...
    params := persistentvolumecontroller.ControllerParameters{
        KubeClient:                ctx.ClientBuilder.ClientOrDie("persistent-volume-binder"),
        SyncPeriod:                ctx.ComponentConfig.PersistentVolumeBinderController.PVClaimBinderSyncPeriod.Duration,
        VolumeInformer:            ctx.InformerFactory.Core().V1().PersistentVolumes(),
        ClaimInformer:             ctx.InformerFactory.Core().V1().PersistentVolumeClaims(),
        ClassInformer:             ctx.InformerFactory.Storage().V1().StorageClasses(),
        PodInformer:               ctx.InformerFactory.Core().V1().Pods(),
        NodeInformer:              ctx.InformerFactory.Core().V1().Nodes(),
        EnableDynamicProvisioning: ctx.ComponentConfig.PersistentVolumeBinderController.EnableDynamicProvisioning,
    }
    volumeController, volumeControllerErr := persistentvolumecontroller.NewController(params)
    // ...
    go volumeController.Run(ctx.Stop)
}
```

### NewController：实例化控制器

`NewController`（`pv_controller_base.go:79`）创建 `PersistentVolumeController` 对象：

```go
// pkg/controller/volume/persistentvolume/pv_controller_base.go:79
func NewController(p ControllerParameters) (*PersistentVolumeController, error) {
    // ...
    controller := &PersistentVolumeController{
        volumes:           newPersistentVolumeOrderedIndex(),
        claims:            cache.NewStore(cache.DeletionHandlingMetaNamespaceKeyFunc),
        kubeClient:        p.KubeClient,
        eventRecorder:     eventRecorder,
        runningOperations: goroutinemap.NewGoRoutineMap(true /* exponentialBackoffOnError */),
        // ...
        claimQueue:  workqueue.NewNamed("claims"),   // PVC 工作队列
        volumeQueue: workqueue.NewNamed("volumes"),  // PV 工作队列
    }
```

两个 workqueue（`claimQueue` 和 `volumeQueue`）是双 Worker 架构的核心，分别处理 PVC 和 PV 事件。

### VolumeInformer 事件回调

```go
// pv_controller_base.go（简化）
p.VolumeInformer.Informer().AddEventHandler(cache.ResourceEventHandlerFuncs{
    AddFunc:    func(obj interface{}) { controller.enqueueWork(controller.volumeQueue, obj) },
    UpdateFunc: func(old, new interface{}) { controller.enqueueWork(controller.volumeQueue, new) },
    DeleteFunc: func(obj interface{}) { controller.enqueueWork(controller.volumeQueue, obj) },
})
```

三个事件（add/update/delete）统一塞入 `volumeQueue`，Worker 根据对象是否还在 Informer 缓存中判断是 add/update 还是 delete。

ClaimInformer 的回调逻辑相同，事件塞入 `claimQueue`。

StorageClass / Pod / Node 的 Informer 只做 Hasynced 同步，不入队（只用于 `syncClaim` 时做辅助查询）。

### InitPlugins：初始化存储插件体系

```go
// pkg/volume/plugins.go:594
func (pm *VolumePluginMgr) InitPlugins(plugins []VolumePlugin, prober DynamicPluginProber, host VolumeHost) error {
    // allPlugins 合并 in-tree + CSI 迁移插件
    allPlugins := append(plugins, ...)
    for _, plugin := range allPlugins {
        name := plugin.GetPluginName()
        // 校验 name 合法性
        if err = plugin.Init(host); err != nil {
            // 初始化失败：记录 error，跳过（不 fatal）
            klog.Errorf("Failed to load volume plugin %q, skipping: %v", name, err)
            continue
        }
        pm.plugins[name] = plugin   // 塞入 plugins map
    }
}
```

每个 volume 插件实现 `VolumePlugin` 接口，`Init` 方法接收 `VolumeHost`（提供访问 kubelet/apiserver 的能力）。后续 `syncVolume` 和 `syncClaim` 通过 `findProvisionablePlugin` / `findDeletablePlugin` 从这个 map 中查找对应插件。

### ctrl.Run：启动主循环

```go
// pv_controller_base.go:303
func (ctrl *PersistentVolumeController) Run(stopCh <-chan struct{}) {
    defer utilruntime.HandleCrash()
    defer ctrl.claimQueue.ShutDown()
    defer ctrl.volumeQueue.ShutDown()

    // 等待所有 Informer 完成首次 List
    if !cache.WaitForNamedCacheSync("persistent volume", stopCh,
        ctrl.volumeListerSynced, ctrl.claimListerSynced, ...) {
        return
    }

    // 用 Informer 数据填充 PV/PVC 本地缓存（避免第一次 sync 时 cache miss）
    ctrl.initializeCaches(ctrl.volumeLister, ctrl.claimLister)

    // 启动三个 goroutine
    go wait.Until(ctrl.resync,       ctrl.resyncPeriod, stopCh)  // 定期全量重新入队
    go wait.Until(ctrl.volumeWorker, time.Second,      stopCh)  // 处理 PV
    go wait.Until(ctrl.claimWorker,  time.Second,      stopCh)  // 处理 PVC
}
```

`resync` 定期把全部 PV/PVC 重新 enqueue，保证即使事件丢失也能最终收敛（类似 ReplicaSet controller 的 resync 机制）。

---

## §04 PV 控制器源码解读之 volumeWorker 处理 PV 增删

| 读码目标 | 源文件（可点击） | 入口函数 |
|---------|----------------|---------|
| PV Worker | [pv_controller_base.go](kubernetes/pkg/controller/volume/persistentvolume/pv_controller_base.go) | `volumeWorker:411` |
| PV 同步核心 | [pv_controller.go](kubernetes/pkg/controller/volume/persistentvolume/pv_controller.go) | `syncVolume:542` |
| PV 绑定 | [pv_controller.go](kubernetes/pkg/controller/volume/persistentvolume/pv_controller.go) | `bind:1005` |

### volumeWorker 主循环

```go
// pv_controller_base.go:411
func (ctrl *PersistentVolumeController) volumeWorker() {
    workFunc := func() bool {
        keyObj, quit := ctrl.volumeQueue.Get()
        if quit { return true }
        defer ctrl.volumeQueue.Done(keyObj)

        key := keyObj.(string)
        _, name, err := cache.SplitMetaNamespaceKey(key)

        volume, err := ctrl.volumeLister.Get(name)
        if volume == nil {
            // 对象不在 informer cache 中 → 已被删除
            ctrl.deleteVolume(volume)
        } else {
            // 对象仍在 → add/update/sync
            ctrl.updateVolume(volume)
        }
    }
    for { if quit := workFunc(); quit { return } }
}
```

`updateVolume` 先更新本地缓存版本号（`storeVolumeUpdate`），再调 `syncVolume`；若版本号未更新（old version）则跳过。

### syncVolume：PV 状态机

`syncVolume`（`pv_controller.go:542`）是 PV 同步的核心，是一个多分支状态机：

```
syncVolume(volume)
  │
  ▼ 更新 migration annotations（CSI 迁移相关）
  │
  ├─── volume.Spec.ClaimRef == nil（PV 未绑定）
  │     │  PV 处于 Available，寻找匹配 PVC
  │     └─── updateVolumePhase → phase = Available
  │
  ├─── volume.Spec.ClaimRef != nil（PV 已声明绑定关系）
  │     │
  │     ├─── PVC 存在于 informer cache 中
  │     │     ├─── volume.Status.Phase != Bound → updateVolumePhase → Bound
  │     │     └─── PVC.UID 与 ClaimRef.UID 匹配 → 同步状态正常
  │     │
  │     ├─── PVC 不在 informer cache（已删除）
  │     │     └─── volume.Status.Phase == Released → reclaimVolume(volume)
  │     │
  │     └─── PVC 绑定到其他 PV（ClaimRef.UID 不匹配）
  │           └─── unbindVolume(volume)  重置 PV 为 Available
  │
  └─── 动作04：PV 已绑定 PVC，但 PVC 绑定到其他 PV → unbindVolume，重置
```

**关键分支——Released 触发 reclaimVolume**：

当 PVC 被删除后，PV 的 `Status.Phase` 变为 `Released`，下一次 `syncVolume` 时会进入 `reclaimVolume` 分支。

### bind：原子绑定操作

```go
// pv_controller.go:1005
func (ctrl *PersistentVolumeController) bind(volume *v1.PersistentVolume, claim *v1.PersistentVolumeClaim) error {
    // bindVolumeToClaim：设置 volume.Spec.ClaimRef 指向 pvc
    // bindClaimToVolume：设置 claim.Spec.VolumeName 指向 pv
    // updateVolumePhase → Bound
    // updateClaimStatus → Bound
}
```

`bind` 分两步写入 apiserver：先更新 PV（写 ClaimRef），再更新 PVC（写 VolumeName）。两步之间如果控制器崩溃，`resync` 会在下次启动时重新触发 `syncVolume`，发现 PV 已有 ClaimRef 但 PVC 未更新，会重新完成绑定（幂等设计）。

---

## §05 PV 控制器源码解读之 reclaimVolume 回收解析

| 读码目标 | 源文件（可点击） | 入口函数 |
|---------|----------------|---------|
| 回收策略路由 | [pv_controller.go](kubernetes/pkg/controller/volume/persistentvolume/pv_controller.go) | `reclaimVolume:1089` |
| Recycle 操作 | [pv_controller.go](kubernetes/pkg/controller/volume/persistentvolume/pv_controller.go) | `recycleVolumeOperation:1135` |
| Delete 操作 | [pv_controller.go](kubernetes/pkg/controller/volume/persistentvolume/pv_controller.go) | `deleteVolumeOperation:1231` |
| Delete 执行 | [pv_controller.go](kubernetes/pkg/controller/volume/persistentvolume/pv_controller.go) | `doDeleteVolume:1404` |

### reclaimVolume：三策略路由

```go
// pv_controller.go:1089
func (ctrl *PersistentVolumeController) reclaimVolume(volume *v1.PersistentVolume) error {
    // ...
    switch volume.Spec.PersistentVolumeReclaimPolicy {
    case v1.PersistentVolumeReclaimRetain:
        // 只打日志，不做任何操作
        klog.V(4).Infof("reclaimVolume[%s]: policy is Retain, nothing to do", volume.Name)

    case v1.PersistentVolumeReclaimRecycle:
        // 异步启动 recycleVolumeOperation
        ctrl.scheduleOperation(opName, func() error {
            return ctrl.recycleVolumeOperation(volume)
        })

    case v1.PersistentVolumeReclaimDelete:
        // 异步启动 deleteVolumeOperation
        ctrl.scheduleOperation(opName, func() error {
            _, err = ctrl.deleteVolumeOperation(volume)
            return err
        })
    }
}
```

**Retain**：仅记日志。PV 保持 Released 状态，管理员需要手动清理数据并删除/重建 PV。

### Recycle 策略：启动 recycler pod

```go
// pv_controller.go:1135
func (ctrl *PersistentVolumeController) recycleVolumeOperation(volume *v1.PersistentVolume) {
    // double check：PV 仍是最新版本
    newVolume, err := ctrl.kubeClient.CoreV1().PersistentVolumes().Get(ctx, volume.Name, ...)

    // 检查 PV 是否仍需 recycle（Phase == Released）
    if volume.Status.Phase != v1.VolumeReleased { return }

    // 查找对应 Provisioner 插件
    plugin, err := ctrl.volumePluginMgr.FindRecyclablePluginBySpec(volume.Spec)

    // 调用插件的 Recycle 方法，内部会创建 recycler pod
    recycler, err := plugin.NewRecycler(pvName, spec, eventRecorder)
    err = recycler.Recycle()

    // Recycle 成功后：unbindVolume → PV 重回 Available
    if err == nil {
        klog.V(4).Infof("reclaimVolume[%s]: volume recycled", volume.Name)
        ctrl.updateVolumePhase(volume, v1.VolumeAvailable, "")
    }
}
```

**recycler pod 的实现**（`plugins.go`）：

```go
// pkg/volume/plugins.go（简化）
func NewPersistentVolumeRecyclerPodTemplate() *v1.Pod {
    pod := &v1.Pod{
        Spec: v1.PodSpec{
            RestartPolicy: v1.RestartPolicyNever,
            Volumes: []v1.Volume{{
                Name: "vol",
                // VolumeSource 由具体插件覆盖（指向待回收的 PV）
                VolumeSource: v1.VolumeSource{},
            }},
            Containers: []v1.Container{{
                Name:    "pv-recycler",
                Image:   "busybox:1.27",
                Command: []string{"/bin/sh"},
                Args:    []string{"-c", "test -e /scrub && rm -rf /scrub/..?* /scrub/.[!.]* /scrub/*"},
                VolumeMounts: []v1.VolumeMount{{
                    Name:      "vol",
                    MountPath: "/scrub",
                }},
            }},
        },
    }
}
```

本质：在 `/scrub` 目录下执行 `rm -rf`，清空数据。Pod 跑完后由 `internalRecycleVolumeByWatchingPodUntilCompletion` 监听完成并清理 recycler pod。

### Delete 策略：插件删除后端存储

```go
// pv_controller.go:1231
func (ctrl *PersistentVolumeController) deleteVolumeOperation(volume *v1.PersistentVolume) (string, error) {
    // double check：重新从 apiserver 读取最新版本
    newVolume, err := ctrl.kubeClient.CoreV1().PersistentVolumes().Get(...)
    if newVolume.Status.Phase != v1.VolumeReleased { return }

    // 调 doDeleteVolume 执行删除
    pluginName, deleted, err := ctrl.doDeleteVolume(volume)
    if deleted {
        // 删除成功，从 etcd 中删除 PV 对象
        ctrl.kubeClient.CoreV1().PersistentVolumes().Delete(...)
    }
}

// pv_controller.go:1404
func (ctrl *PersistentVolumeController) doDeleteVolume(volume *v1.PersistentVolume) (string, bool, error) {
    // 查找对应 DeletableVolumePlugin
    plugin, err := ctrl.volumePluginMgr.FindDeletablePluginBySpec(volume.Spec)

    // 构造 Deleter 对象并执行 Delete
    deleter, err := plugin.NewDeleter(spec)
    err = deleter.Delete()   // 调用插件删除后端存储（如 rm -rf /tmp/xxx for hostPath）
}
```

以 `hostPath` deleter 为例：

```go
func (r *hostPathDeleter) Delete() error {
    regexp := regexp.MustCompile("/tmp/.+")
    if !regexp.MatchString(r.GetPath()) {
        return fmt.Errorf("host_path deleter only supports /tmp/.+ but received provided path: %v", r.GetPath())
    }
    return os.RemoveAll(r.GetPath())
}
```

---

## §06 PV 控制器源码解读之 claimWorker 处理 PVC

| 读码目标 | 源文件（可点击） | 入口函数 |
|---------|----------------|---------|
| PVC Worker | [pv_controller_base.go](kubernetes/pkg/controller/volume/persistentvolume/pv_controller_base.go) | `claimWorker:469` |
| PVC 同步入口 | [pv_controller.go](kubernetes/pkg/controller/volume/persistentvolume/pv_controller.go) | `syncClaim:252` |
| 未绑定 PVC 处理 | [pv_controller.go](kubernetes/pkg/controller/volume/persistentvolume/pv_controller.go) | `syncUnboundClaim:333` |
| 已绑定 PVC 处理 | [pv_controller.go](kubernetes/pkg/controller/volume/persistentvolume/pv_controller.go) | `syncBoundClaim:475` |
| 动态创建 PV | [pv_controller.go](kubernetes/pkg/controller/volume/persistentvolume/pv_controller.go) | `provisionClaim:1442` |

### claimWorker → syncClaim

`claimWorker`（`pv_controller_base.go:469`）结构与 `volumeWorker` 完全对称：消费 `claimQueue`，add/update → `updateClaim` → `syncClaim`，delete → `deleteClaim`。

`syncClaim`（`pv_controller.go:252`）先更新 migration annotations，然后按 PVC 是否已绑定分两条路径：

```go
// pv_controller.go:252
func (ctrl *PersistentVolumeController) syncClaim(claim *v1.PersistentVolumeClaim) error {
    // 更新 migration annotations
    newClaim, err := ctrl.updateClaimMigrationAnnotations(claim)
    claim = newClaim

    if !metav1.HasAnnotation(claim.ObjectMeta, pvutil.AnnBindCompleted) {
        return ctrl.syncUnboundClaim(claim)   // 未绑定
    } else {
        return ctrl.syncBoundClaim(claim)     // 已绑定
    }
}
```

### syncUnboundClaim：未绑定 PVC 处理

```
syncUnboundClaim(claim)
  │
  ├─── claim.Spec.VolumeName == ""（未指定 PV）
  │     │
  │     ├─── IsDelayBindingMode → 延迟绑定，emitEvent 通知调度器
  │     │
  │     ├─── findBestMatchForClaim → 在现有 PV 中找满足条件的 PV（静态匹配）
  │     │     ├─── 找到 PV → bind(volume, claim)
  │     │     └─── 找不到 PV
  │     │           ├─── storageClass != "" → provisionClaim(claim)（动态创建）
  │     │           └─── 否则 → updateClaimStatus → Pending，等下次 resync
  │     │
  │     └─── 静态绑定成功 → bindVolumeToClaim + bindClaimToVolume
  │
  └─── claim.Spec.VolumeName != ""（用户指定了特定 PV）
        ├─── 找到指定 PV，PV 处于 Available → bind
        ├─── PV 不存在 → updateClaimStatus → Pending
        └─── PV 已被其他 PVC 占用 → event + Pending
```

**静态匹配逻辑（findBestMatchForClaim）**：遍历所有 Available 状态的 PV，根据 AccessMode、容量、storageClass、selector 逐一过滤，选出满足条件且容量最小（Best Fit）的 PV，减少存储碎片。

### provisionClaim：动态创建 PV

```go
// pv_controller.go:1442
func (ctrl *PersistentVolumeController) provisionClaim(claim *v1.PersistentVolumeClaim) error {
    // 查找对应插件
    plugin, storageClass, err := ctrl.findProvisionablePlugin(claim)
    // 异步执行（避免阻塞 Worker）
    ctrl.scheduleOperation(opName, func() error {
        _, err = ctrl.provisionClaimOperation(claim, plugin, storageClass)
        return err
    })
}
```

`provisionClaimOperation`（`pv_controller.go:1481`）：

```go
// 构造 VolumeOptions（ReclaimPolicy、MountOptions、ClusterName、PVC 等）
options := vol.VolumeOptions{
    PersistentVolumeReclaimPolicy: *storageClass.ReclaimPolicy,
    MountOptions:                  storageClass.MountOptions,
    CloudTags:                     &tags,
    ClusterName:                   ctrl.clusterName,
    PVName:                        pvName,
    PVC:                           claim,
    Parameters:                    storageClass.Parameters,
}

// 调用插件的 Provision 方法动态创建后端存储并返回 PV 对象
volume, err = plugin.Provision(options)

// 创建 PV 对象写入 apiserver
_, err = ctrl.kubeClient.CoreV1().PersistentVolumes().Create(ctx, volume, ...)
```

以 `hostPath` provisioner 为例，它会在 `/tmp` 下创建一个目录，然后返回一个指向该目录的 PV 对象（仅限测试用途）。

**外部 Provisioner（external-provisioner）**：生产环境中，NFS/Ceph 等存储通常使用外部 Provisioner（独立部署的 Deployment），它监听 PVC 事件，在 storageClass 匹配时创建实际的存储资源，并通过 apiserver 写入 PV 对象。命名规则：`{namespace}-{pvcName}-{pvName}`。

### syncBoundClaim：已绑定 PVC 处理

```go
// pv_controller.go:475
func (ctrl *PersistentVolumeController) syncBoundClaim(claim *v1.PersistentVolumeClaim) error {
    // 判断 PVC 绑定的 PV 是否存在
    obj, found, err := ctrl.volumes.store.GetByKey(claim.Spec.VolumeName)

    if !found {
        // PV 不存在 → 触发 ClaimLost event
        ctrl.updateClaimStatusWithEvent(claim, v1.ClaimLost, nil, v1.EventTypeWarning, ...)
        return nil
    }

    volume, ok := obj.(*v1.PersistentVolume)

    if volume.Spec.ClaimRef == nil {
        // PV 存在但未绑定（PVC 是 Bound 但 PV 来 Unbound）
        // 重新 bind
        ctrl.bind(volume, claim)
        return nil
    }

    if volume.Spec.ClaimRef.UID == claim.UID {
        // 正常状态：互相绑定，更新状态即可
        ctrl.bind(volume, claim)
        return nil
    } else {
        // 两个 PVC 绑定到同一个 PV → ClaimLost
        ctrl.updateClaimStatusWithEvent(claim, v1.ClaimLost, nil, v1.EventTypeWarning, ...)
        return nil
    }
}
```

`syncBoundClaim` 的核心是检查"PVC 认为自己绑定的 PV 是否还承认这段关系"。异常情况（PV 不存在、PV 被抢占）都触发 `ClaimLost`，通知上层重新调度。
