# 第30章 k8s 网络底层原理

> **适用版本**: Kubernetes v1.21
> **对应章节**: 第 30 章 — k8s 网络底层原理
> **源码入口**: `pkg/kubelet/dockershim/docker_sandbox.go:89`（`RunPodSandbox`）、`pkg/kubelet/dockershim/network/cni/cni.go:302`（`SetUpPod`）

---

## 核心机制一览

1. **四种通信模式，三层网络**：Kubernetes 需要解决四种通信：容器间通信、Pod 间通信、Pod↔Service 通信、Internet↔Service 通信。底层存在三层网络：节点网络（物理/虚拟机网络）、Pod 网络（CNI 管理）、Service 网络（iptables/ipvs 管理）。

2. **同 Pod 内容器共享 Network Namespace**：每个 Pod 有一个 pause（Infra）容器先启动并持有 Network Namespace，其他容器通过 Join Namespace 加入，共享同一 IP 和端口空间——容器间通过 `localhost` 互访，多容器不能 listen 相同端口。

3. **同节点 Pod 间通信走网桥（Flannel）或路由（Calico）**：Flannel 模式下 Pod 默认路由是 `docker0/cni0` 网桥，同节点 Pod 直接通信；Calico 模式下同节点 Pod 间走路由转发（`caliXXX` veth pair），不用网桥。

4. **跨节点 Pod 间通信区别**：Flannel 用隧道封装（overlay），flanneld 负责封包/解包；Calico 用纯三层路由（BGP），中间没有 NAT，数据包不需要封装，性能更好。

5. **Pod 访问 Service 三步骤**：01 CoreDNS 解析 Service VIP → 02 节点 iptables/kube-proxy 转发（`PREROUTING → KUBE-SERVICES → KUBE-SVC-xxx → KUBE-SEP-xxx → DNAT`）→ 03 不同节点 Pod 间通信走 Calico 路由。

6. **CNI 是 kubelet 与网络插件之间的标准接口**：kubelet 创建 sandbox container 时调用 CRI（docker-shim）→ CRI 加载 `/etc/cni/net.d` 配置 → 准备参数（环境变量 + 配置文件）→ 调用 CNI 二进制插件（如 `calico ADD`）→ 插件为 sandbox 创建 veth pair、分配 IP、配置路由。

---

## 全章调用链总图

```
Pod 创建
  │
  ▼ kubelet → CRI(docker-shim)
RunPodSandbox (docker_sandbox.go:89)
  │
  ├─── 创建 sandbox container (pause 容器)
  │
  └─── network.SetUpPod (network/plugins.go:410)
          │
          ▼ PluginManager.SetUpPod
       cniNetworkPlugin.SetUpPod (cni/cni.go:302)
          │
          └─── addToNetwork (cni/cni.go:350)
                  │
                  ▼ libcni.AddNetworkList
               调用 calico 二进制插件 (ADD 命令)
                  │
                  ▼ calico plugin.go:cmdAdd
               CmdAddK8s (linuxDataplane.DoNetworking)
                  │
                  ├─── 创建 veth pair (caliXXX ↔ eth0)
                  ├─── 设置 MAC 地址
                  ├─── calico-ipam 分配 IP
                  ├─── 配置容器端默认路由 (169.254.1.1)
                  ├─── 配置宿主机端路由 (10.x.x.x → caliXXX)
                  ├─── 配置 arp proxy + packet forwarding
                  └─── 写入 WorkloadEndpoint 到 calico datastore
```

---

## §01 同 Pod 容器和容器之间的通信

### k8s 四种通信模式与三层网络

```
                    Service 网络
         ┌──────────────────────────────┐
         │        Pod 网络              │
         │  ┌──────────────────────┐    │
         │  │ master               │    │
         │  └──────────────────────┘    │
         │  ┌───────────────────────┐   │
         │  │ node  Pod Pod kubeproxy│  │
         │  └───────────────────────┘   │
         │  ┌───────────────────────┐   │
         │  │ node  Pod Pod kubeproxy│  │
         │  └───────────────────────┘   │
         └──────────────────────────────┘
                    节点网络
```

Kubernetes 需要解决四种通信模式：
1. 容器和容器之间的通信
2. Pod 和 Pod 之间的通信
3. Pod 和 Service 之间的通信
4. Internet 和 Service 之间的通信

### 同 Pod 内容器通信：共享 Network Namespace

Pod 里的多个容器怎么去共享网络？在 Kubernetes 中，每个 Pod 都有一个 pause（Infra）容器，所有其他容器通过 `Join Namespace` 的方式加入到 Infra container 的 Network Namespace 中。

**Infra container 的作用**：
- 由于需要一个相当于说中间的容器存在，所以整个 Pod 里面必然是 Infra container 第一个启动
- 整个 Pod 的生命周期等同于 Infra container 的生命周期，与容器 A 和 B 无关
- 这也是为什么在 Kubernetes 里可以单独更新 Pod 里的某一个镜像，整个 Pod 不会重建、也不会重启

```
          IPC
        ┌──────────────────────────────┐
        │  Network                     │
        │  ┌──────────────────────┐    │
        │  │  PID                 │    │
        │  │  ┌────────────────┐  │    │
        │  │  │ Hostname pause │  │    │
        │  └──┼────────────────┼──┘    │
        │     │  nginx localhost ghost │
        └─────┼──────────────────┼────┘
              └──────────────────┘
```

**验证：同 Pod 内不能 listen 相同端口**：

```yaml
# 两个都设置 containerPort: 80 的 nginx，Pod 状态为 Error（端口冲突）
containers:
- name: nginx1
  image: nginx:1.7.9
  ports:
  - containerPort: 80
- name: nginx2
  image: nginx:1.8
  ports:
  - containerPort: 80
```

**验证：同 Pod 内通过 localhost 互访**：

```yaml
# nginx + curl 容器在同一 Pod
containers:
- name: curl
  image: yauritux/busybox-curl
- name: nginx
  image: nginx:1.8
```

`kubectl exec onepod-multicontiner02 -c curl -- curl 127.0.0.1/www` 能访问到 nginx，验证容器间共享网络空间。

---

## §02 Pod 和 Pod 之间的通信

### 同节点 Pod 间通信

**Flannel 模式**：同一 Node 中 Pod 的默认路由都是 `docker0`（`cni0`）网桥的地址，由于关联在同一 docker0 网桥上，地址网段相同，所有它们之间应当是能直接通信的。

```
         Node1
  ┌─────────────────────┐
  │  POD1         POD2  │
  │  容器1 容器2  容器1 容器2│
  │  共享网络空间  共享网络空间│
  │  eth0(IP1)   eth0(IP2)│
  │   ethX        ethX   │
  │      Docker0网桥(IP3) │
  │           路由        │
  │          eth0         │
  └─────────────────────┘
```

**Calico 模式**：同一 node 上的路由转发，每个 Pod 通过 veth pair（`caliXXX`）接入宿主机路由表，Pod 间直接走路由。

```
         192.168.1.2                  192.168.1.3
    ┌─────────────────┐          ┌─────────────────┐
    │ BGP    Felix    │          │ BGP    Felix    │
    │Client           │          │Client           │
    │  cali9c02e56 calib4963f3  │  cali8c01e76 calib5863f3│
    │  eth0    eth0   │          │  eth0    eth0   │
    │  Cont.1  Cont.2 │          │  Cont.3  Cont.4 │
    │ 10.233.1.2 Node1 10.233.1.3│ 10.233.2.2 Node2 10.233.2.3│
    └─────────────────┘          └─────────────────┘
```

同节点 Pod 间链路：`pod1的eth0 → cli-1网卡 → 路由 → cli-2网卡 → pod2的eth0`

### 跨节点 Pod 间通信

**Flannel 模式**（隧道封包）：
- pod 中产生数据，根据 pod 的路由信息，将数据发送到 Cni0
- Cni0 根据节点的路由表，将数据发送给隧道设备 flannel.1
- flannel.1 查看数据包的目的 ip，从 flanneld 获得对端隧道设备的必要信息，封装数据包
- flannel.1 将数据包发送到对端设备，对端节点的网卡接收到数据包，发现数据为 overlay 数据包，解开外层封装，并发送内层封装到 flannel.1 设备
- flannel.1 设备查看数据包，根据路由匹配，将数据包发送给 Cni0 设备；Cni0 匹配路由表，发送数据给网桥上对应的端口

```
  pod1        pod2              Etcd
  addr        addr
   │           │                │
   ▼           ▼                │
  cni0     flannel.1 ← flanneld─┘
   │           │
  eth0        eth0
   │           │
  eth0        eth0
   │           │
  flannel.1   cni0
               │
             pod3  pod4
```

**Calico 模式**（纯三层路由，无 NAT）：
- `pod1的eth0 → cli-1网卡 → node的eth0/Flannel → node2的eth0 → cli-2网卡 → pod2的eth0`
- Calico 不会在宿主机上创建任何网桥设备，利用 BGP 在各节点上广播路由
- 每个 Calico CNI 节点为每个容器都创建一对 Veth Pair 设备，然后把其中一端放到宿主机上（命名如 `cali9c02e56`），另一端放到容器里（命名为 `eth0`）

---

## §03 Pod 和 Service 之间的通信

### 通信三步骤全流程图

```
             01.svc域名解析到vip             02.iptables计算
                                            node上iptables规则
client-pod                                 KUBE-SERVICES链
ip 10.100.85.247        CoreDNS            KUBE-SVC-xxx
    │                   解析svc vip         KUBE-SEP-xxx
    │ 请求svc域名         10.96.125.237        │
nginx-pod08.default.svc.cluster.local:8080  │ DNAT
                                    ┌────────┘
                                    │ pod 10.100.85.245
                                    │ pod 10.100.85.246
                                    │ pod 10.100.85.253
                    03.pod之间通信(路由)
         确定目标Pod的podIP
         10.100.85.247的veth网卡
         配置好路由到node上
```

### 步骤 01：CoreDNS 解析 svc 的 vip

创建 Deployment + ClusterIP Service，然后在 curl Pod 中：

```bash
curl nginx-pod08.default.svc.cluster.local:8080
# CoreDNS 解析返回 ClusterIP 10.96.125.237
```

### 步骤 02：iptables 转发

iptables 规则链路：`PREROUTING → KUBE-SERVICES → KUBE-SVC-xxx → KUBE-SEP-xxx`

```bash
# KUBE-SERVICES 链：匹配 ClusterIP 10.96.125.237 8080 端口
-A KUBE-SERVICES -d 10.96.125.237/32 --dport 8080 -j KUBE-SVC-DDZHMKSA03F6KIZ

# KUBE-SVC-xxx 链：随机选 endpoint（probability 0.5）
-A KUBE-SVC-DDZHMKSA03F6KIZ -m statistic --mode random --probability 0.5 -j KUBE-SEP-FXZXKRS4RXLGILDS
-A KUBE-SVC-DDZHMKSA03F6KIZ -j KUBE-SEP-FXZXKRSaRXLGILDS

# KUBE-SEP-xxx 链：DNAT 到具体 Pod
-A KUBE-SEP-FXZXKRS4RXLGILDS -j DNAT --to-destination 10.100.85.253:80
```

KUBE-MARK-MASQ 打标记，最后在 POSTROUTING 做 MASQUERADE（SNAT），回包地址改为 node 的 IP。

### 步骤 03：不同 pod 之间走 Calico 路由

完成 DNAT 后，数据包目的地已经是具体 Pod IP（如 `10.100.85.240:80`），走 Calico 路由到目标节点。

### Service 到底能不能 ping 通

| Service 类型 | iptables 模式 | ipvs 模式 |
|-------------|--------------|----------|
| ClusterIP/NodePort | 不能 ping（iptables 只处理 ip:port 四层数据包，reject icmp） | 能 ping（ClusterIP 绑到 kube-ipvs0 虚拟网卡，icmp 包到 lo 回环） |
| Headless（ClusterIP=None） | DNS 直接返回 Pod IP 的 A 记录，随机选一个，能 ping | 同左 |
| LoadBalancer | 普通模式基于 NodePort；直连模式 LB 与 Pod 同 VPC，ping 结果与 Headless 一致 |
| ExternalName | 对应 DNS CNAME，配置的域名能 ping 通则 svc 可以 ping 通 |

### iptables 五条链的含义

| 链 | 触发时机 |
|----|---------|
| PREROUTING | 流入本机路由前 |
| POSTROUTING | 流出本机路由前 |
| FORWARD | 转发路径 |
| OUTPUT | 由本机用户程序发出的 |
| INPUT | 发送至本机用户程序的 |

**SNAT vs DNAT**：
- **SNAT**（源地址转换）：将报文发送方的 IP 地址转换，使相应方回复时回复的是发送方的地址。当 client 发送请求给 server 时，需要经过 gateway；如果 gateway 不对包进行 SNAT，server 会回应 client 地址，但 client 不认识 server，导致错误。
- **DNAT**（目标地址转换）：将报文的目标地址转换，起到请求转发到别的目的地的作用。

---

## §04 k8s CNI 简介和 kubelet 中 CNI 源码调用

| 读码目标 | 源文件（可点击） | 入口函数 |
|---------|----------------|---------|
| RunPodSandbox | [docker_sandbox.go](kubernetes/pkg/kubelet/dockershim/docker_sandbox.go) | `RunPodSandbox:89` |
| CNI SetUpPod | [cni.go](kubernetes/pkg/kubelet/dockershim/network/cni/cni.go) | `SetUpPod:302` |
| addToNetwork | [cni.go](kubernetes/pkg/kubelet/dockershim/network/cni/cni.go) | `addToNetwork:350` |

### CNI 介绍

CNI（Container Network Interface）是 Google 和 CoreOS 联合制定的网络标准，基于 rkt 实现多容器通信的网络模型。

**CNI 规范**：
- 各厂商网络解决方案不能全都集成进 kubelet，CNI 就是能让各网络厂商对接进来的接口
- 容器就是 linux network namespace
- CNI 网络定义以 json 格式存储
- 有关网络的配置通过 STDIN 传递给 CNI 插件，其他参数通过环境变量传递
- CNI 插件以可执行文件方式实现

**CNI 原理两个部分**：
- 二进制插件配置 POD 的网络栈（runtime）：给 POD 插上网线
- Daemon 进程实现网络互通（plugin）：给 POD 连上网络

**CNI 插件三类**：
- Main 类：在宿主机上创建网桥并为 veth pair 等虚拟设备创建网络桥接（bridge、ipvlan、macvlan、loopback、ptp、vlan）
- IPAM 类（IP Address Management）：分配 IP（dhcp、host-local、static）
- Meta 类：调用其他插件（flannel、tuning、portmap、bandwidth）

### CNI 工作流（从 kubelet 到插件调用）

```
在宿主机安装网络方案本身
  │
  ▼
kubelet → CRI(docker-shim) → 加载 /etc/cni/net.d 下的插件
  │
  ▼
准备 CNI 插件参数
  ├─── 1. CRI 设置的一组环境变量
  └─── 2. CRI 从 CNI 配置中加载信息（/etc/cni/net.d/10-calico.conflist）
  │
  ▼
传递给 CNI 插件
  │
  ▼
执行将容器加入网络操作
```

### CNI 配置文件解析（10-calico.conflist）

```json
{
  "name": "k8s-pod-network",
  "cniVersion": "0.3.1",
  "plugins": [
    {
      "type": "calico",
      "datastore_type": "kubernetes",
      "mtu": 0,
      "ipam": {"type": "calico-ipam", "assign_ipv4": "true", "assign_ipv6": "false"},
      "policy": {"type": "k8s"},
      "kubernetes": {
        "k8s_api_root": "https://10.96.0.1:443",
        "kubeconfig": "/etc/cni/net.d/calico-kubeconfig"
      }
    },
    {"type": "bandwidth", "capabilities": {"bandwidth": true}},
    {"type": "portmap", "snat": true, "capabilities": {"portMappings": true}}
  ]
}
```

字段含义：
- `cniVersion`：CNI 规范版本 0.3.1
- `name`：网络名 `k8s-pod-network`
- `plugins` 中：`portmap`（端口映射）、`bandwidth`（TBF 限流）、`calico`（IP 分配）

### kubelet CNI 源码调用链

```
RunPodSandbox (docker_sandbox.go:89)
  │
  └─── ds.network.SetUpPod()
          │
          ▼ (network/plugins.go:410) PluginManager.SetUpPod
       plugin.SetUpPod (cni/cni.go:302)
          │
          └─── plugin.addToNetwork(ctx, plugin.getDefaultNetwork(), ...)
                  │
                  ▼ (cni/cni.go:350)
               net.addToNetwork → libcni.AddNetworkList
                  │
                  └─── c.addNetwork → invoke.ExecPluginWithResult(ctx, pluginPath, newConf.Bytes, c.args("ADD", rt), c.exec)
                          │
                          └─── 调用 calico 二进制文件（ADD 命令，参数通过环境变量传递）
```

---

## §05 k8s Calico plugin 源码解析

### Calico plugin 入口

calico 代码库在 `github.com/projectcalico/cni-plugin`，实现了 ADD 和 DEL 命令。入口在 `D:\go_path\src\github.com\projectcalico\cni-plugin\pkg\plugin\plugin.go`。

`cmdAdd` 逻辑：
1. `args.StdinData` 解析配置（即 conf-dir 传进来的文件内容），即 cni 配置数据
2. `types.NetConf` 结构体定义 calico CNI 插件所支持的所有配置字段

### CmdAdd 主逻辑

```go
func CmdAdd(args *skel.CmdArgs, conf types.NetConf, ...) {
    // 1. 从 calico datastore 查询是否 ready
    // 2. 获取 WEPIdentifiers（从 kubelet args.IfName 中提取 pod name、namespace 等）
    // 3. endpoint 前缀计算（wepIDs.CalculateWorkloadEndpointName）
    // 4. 通过 calico client 查找已有 endpoint
    // 5. 若 endpoint 不存在，创建新的 WorkloadEndpoint
    // 6. 最终调用 k8s.CmdAddK8s（运行于 kubernetes 时）
}
```

### k8s.CmdAddK8s — 核心：创建 dataplane 并调用 DoNetworking

```go
func CmdAddK8s(ctx, args, conf, epIDs, client, endpoint) {
    // 1. 根据操作系统生成不同 data plane
    d, err := dataplane.GetDataplane(conf, logger)
    // 这里是 linuxDataplane 对象，并创建 k8s client 对象

    // 2. host-local 类型 ipam 逻辑（我们的配置是 calico-ipam）
    // 3. 调用 calico-ipam 插件分配一个 IP 地址
    result, err = utils.AddIPAM(conf, args, logger)

    // 4. 创建 WorkloadEndpoint 对象
    endpoint.Spec.Profiles = profiles
    endpoint.Spec.ServiceAccountName = ...

    // 5. 调用 d.DoNetworking：创建 veth pair + 配置路由
    hostVethName, contVethMac, err = d.DoNetworking(...)

    // 6. 将 WorkloadEndpoint 写入 calico datastore
    _, err = utils.CreateOrUpdate(ctx, caliclient, endpoint)
}
```

### linuxDataplane.DoNetworking — 建立网络的核心

源码在 `D:\go_path\src\github.com\projectcalico\cni-plugin\pkg\dataplane\linux\dataplane_linux.go`

```
DoNetworking
  │
  ▼ 回调每个 pod 的 ns（ns.WithNetNSPath）
  │
  ├─── netlink.Veth：创建 veth pair
  │      Name:     contVethName (容器端，如 eth0)
  │      PeerName: hostVethName (宿主机端，如 cali9c02e56)
  │
  ├─── 设置宿主机端 MAC 为 EE:EE:EE:EE:EE:EE（固定 MAC）
  │
  ├─── ip link set up 宿主机端网卡
  │
  ├─── ip link set up 容器端网卡
  │
  ├─── 容器端添加默认网关路由
  │      default via 169.254.1.1 dev eth0
  │      169.254.1.1 dev eth0 scope link
  │
  ├─── 把 calico-ipam 分配的 pod ip 赋值给容器端 eth0
  │      netlink.AddrAdd(contVeth, netlink.Addr{IPNet: &addr.Address})
  │
  ├─── ip link set up 宿主机端 veth pair，配置宿主机端路由
  │      netlink.LinkSetUp(hostVeth)
  │      SetupRoutes(hostVeth, result)
  │      凡目标地址是 pod ip，数据包进入 caliXXX 网卡
  │      如：10.100.85.192 dev caliab299ca0c34 scope link
  │
  ├─── 配置宿主机端 arp proxy + packet forwarding
  │
  └─── 完成后写入 WorkloadEndpoint 到 calico datastore
```

**总结**：
- calico 二进制插件为一个 sandbox container 创建好了网络资源，即创建了一个 veth pair
- 分别为宿主机端和容器端网卡设置好对应 MAC 地址，以及为容器段配置好了 IP 地址
- 在容器端配置好了路由默认网关（`169.254.1.1`），在宿主机端配置好路由，让目标地址是 sandbox container ip 的进入宿主机 veth pair 网卡
- 为宿主机端网卡配置 arp proxy 和 packet forwarding 功能
- 最后根据这些网络数据生成一个 workloadendpoint 对象存入 calico datastore
