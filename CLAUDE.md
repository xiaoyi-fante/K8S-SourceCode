# Kubernetes 源码学习文档规范

本文件记录源码学习文档的所有写作要求，每次写新文档前阅读此文件。

---

## 最高原则

> **一切写作决策只有一个判断标准：这样写，读者会更容易理解和掌握这个知识点吗？**

这是凌驾于所有具体 rule 之上的元原则。当某条 rule 和这个目的冲突时，以这个目的为准，rule 可以被忽略或打破。

**展示形式（表格、图、散文）是工具，不是规定。**

- 表格好用的时候用表格：场景决策、行为对比、多选项横向比较
- 图好用的时候用图：调用链、时序流程、组件层次关系
- 散文好用的时候用散文：需要因果推导、需要解释"为什么"的地方

判断的唯一依据是：**哪种方式让这个知识点更容易进入读者的脑子。**

---

## 源码学习的特殊上下文

- **学习目标**：理解 Kubernetes 各组件的设计思想、接口约定、执行路径，而不是记忆代码细节
- **版本**：Kubernetes v1.21（源码位于 `K8S-SourceCode/kubernetes/`）
- **源码语言**：Go，文档正文用中文解释，代码引用保留英文
- **文档最终用途**：Markdown 文件（在 VS Code 中阅读），可以使用相对路径链接到源码文件，例如 `[builder.go](kubernetes/staging/src/k8s.io/cli-runtime/pkg/resource/builder.go)`，读者可在编辑器中直接跳转

---

## 文档结构规范

### 灵魂知识点放在文档最开头

每篇文档必须在正文最前面写一个"核心机制一览"章节，列出本文最关键的 N 个知识点。

- 这是"先读这里"的精华，不是总结（总结在最后，这个在最前）
- 内容标准：读者如果只记住这几条，就能理解这篇文档 90% 的价值

格式：用**编号列表加粗标题**，每条格式为 `N. **标题**：正文说明`，不使用表格。

```markdown
1. **cobra.Command 树**：kubectl 的所有子命令都注册为一棵 cobra.Command 树……

2. **Visitor 模式**：Builder 阶段把资源加载、schema 校验、默认值填充……
```

### 禁止写目录

文档中不写目录（Table of Contents）。Notion 有自动目录侧边栏，手写目录是多余的。

### 文件名规范

格式：`<章节号>-<精简主题>.md`，主题从文档核心内容提取，不照搬完整标题。

示例：
- 第02章 kubectl 执行流程与设计模式 → `02-kubectl-执行流程与设计模式.md`

### 标题元信息

每篇文档 H1 标题下方附三行元信息：

```markdown
# 标题

> **适用版本**: Kubernetes v1.21
> **对应章节**: 第 N 章 — 章节名
> **源码入口**: `cmd/kubectl/main.go`（或相关入口文件）
```

---

## ASCII 图规范

### 选择竖排还是横排

| 场景 | 格式 |
|------|------|
| 调用链、时序、执行步骤 | 竖排步骤（`│ ▼` 连线，分支用 `├─── / └───`） |
| 组件拓扑、模块关系 | 横排方框（`┌┐└┘─│` 方框连线） |
| 两条路径对比 | 两段竖排并列，加标题区分 |

**竖排步骤示例（有分支）：**

```
kubectl run
  │
  ▼ cobra 路由到 RunCommand
  │
  ▼ Builder.Do() — 构建 ResourceResult
  │
  ├─── 本地文件（-f）→ FilenameOptions → FileVisitor
  │
  └─── 命令行参数 → ResourceTypeOrNameArgs → Selector
            │
            ▼ 发送 REST 请求到 API Server
```

### 中文与 ASCII 混排对齐

中文字符占 2 列宽，ASCII 字符占 1 列宽。box 内混用时补空格让右侧 `│` 大致对齐。

```
计算方法：行显示宽度 = ASCII 字符数 × 1 + 中文字符数 × 2
目标：各行显示宽度接近 box 顶部边框宽度
```

---

## 内容写作规范

### 重要知识点直接写入文档，不询问

对话中出现的任何重要解释，必须同步用 Edit 工具整合进当前文档，不能只停留在对话中。**不要在回答后问"要写进文档吗？"——判断是重要知识点就直接写。**

- 已有章节：在对应章节内扩充
- 无对应章节：新增小节，放在逻辑上最合适的位置

### 大胆删减重复内容

文档中凡是重复表达同一概念的内容，一律删除。

- 同一机制在"原理"章节和"流程"章节各出现一次 → 保留更深入的那个，删除另一个
- 判断标准：删掉这段，读者还能从文中其他地方学到吗？能 → 删；不能 → 留

### 内容裁剪原则（三类必删）

写完初稿后，对以下三类内容主动删减，不等读者反馈：

**第一类：通用知识——读者可以 Google，不需要在这里**

- 第三方库（cobra、pprof）的通用特性列表，不是 kubectl 特有的
- kubectl 基础操作的输出字段解释（`kubectl get pod` 各列含义等），任何 K8s 入门教程都有
- 判断方法：把这段复制到搜索引擎第一条就能找到 → 删

**第二类：实现细节——超出"理解设计"的必要深度**

- 学习目标是"理解设计思想和执行路径"，不是"记忆每行代码"
- 外围辅助功能（pprof 采集的 switch/case 实现、信号处理）的完整代码 → 只保留"做了什么"的一句话总结，删掉实现
- 判断方法：这段代码体现设计决策吗？不体现 → 删代码留结论

**第三类：跨节重复——总结章不重复正文章**

- 总结节（如 §08）只保留正文各节未曾出现的内容
- 总结节不放流程图（正文已有），不放已有结论的重述
- 判断方法：这段话在文档前面出现过吗？出现过 → 删，改为"见 §XX"

### 源码引用原则

- 引用代码片段时，注明文件路径（相对 `kubernetes/` 根目录）和关键行号，格式：`// pkg/api/types.go:42`
- 只引用与当前知识点直接相关的核心片段，不粘贴整个函数
- 代码块后用中文解释"这段代码在做什么、为什么这样设计"，不只是翻译代码
- 文件路径同时以 Markdown 链接形式提供，路径相对于文档所在目录（`K8S-SourceCode/`），例如：
  ```
  源码：[builder.go](kubernetes/staging/src/k8s.io/cli-runtime/pkg/resource/builder.go)
  ```
  这样读者可在 VS Code 中点击直接跳转到源文件

### 读码入口表

每节正文开头（在叙述段落之前）添加一张读码入口表，格式如下：

```markdown
| 读码目标 | 源文件（可点击） | 入口函数 |
|---------|----------------|---------|
| cobra 根命令构造 | [cmd.go](kubernetes/staging/src/k8s.io/kubectl/pkg/cmd/cmd.go) | `NewKubectlCommand:472` |
| pprof 采集启动  | [profiling.go](kubernetes/staging/src/k8s.io/kubectl/pkg/cmd/profiling.go) | `initProfiling:39` |
```

- 每行对应一个"读者想搞清楚的问题"及其在源码中的定位
- 函数名后跟行号，方便直接 Ctrl+G 跳转
- 只列本节涉及的入口，不求全

### 调用链总图与局部放大图

每章的核心机制一览之后，在正文第一节之前，插入**全章调用链总图**：

- 从用户输入（`kubectl create -f`）到最底层（`RESTClient.Post()`）的完整 ASCII 调用链
- 每个节点标注对应的源码文件名（不用完整路径，文件名够用）
- 风格：竖排步骤图，分支用 `├───` / `└───`

每节（尤其是涉及复杂调用关系的节）在正文开头插入**局部放大图**：

- 只画本节覆盖的那段调用链，相当于"在总图上高亮当前位置"
- 图下方一句话说明"本节从 X 讲到 Y"

**两张图的关系**：总图建立全局心智模型，局部图避免读者在细节里迷路。总图在全章提取阶段完成后统一绘制；局部图在写各节时随手加。

### 架构图解读原则

每张 ASCII 架构图，先判断是否需要图解，再决定写什么。

**先问：图本身 + 周围文字，已经能让读者看懂吗？**
- 能 → 不加图解，直接进入正文
- 不能 → 加 `#### 图解：XXX`，只写图里没有显式传达的信息

图解只写：
- ✅ 图中元素"为什么在这里"、"它的作用是什么"
- ✅ 设计决策的背后逻辑（图呈现"是什么"，图解补充"为什么"）
- ❌ 不能只是把图的标注换个说法重复一遍

---

## 精华内容标准

写文档时应主动挖掘以下内容，而不是等读者追问：

1. **接口先解释清楚**：出现 interface 时，先说清楚它抽象的是什么、谁实现了它、为什么需要这层抽象
2. **设计模式要点明**：看到 Visitor、Builder、Factory 等模式，要说清楚用这个模式解决的是什么问题
3. **调用方向要明确**：多个组件交互时，要说清楚谁调用谁、数据流向是什么
4. **同步 vs 异步要点明**：凡是涉及 goroutine、channel、informer、queue，要说清楚为什么异步、边界在哪里
5. **错误路径也要写**：核心流程的错误处理不是细节，是设计的一部分，要写

---

## 语言规范

- 文档正文：中文
- 代码、函数名、接口名、包名：保留英文原文
- 图表下方的解释说明：中文

---

## 写作流程（从截图到文档）

用户没有文字课件，只能从视频截图。文档整理分两个阶段进行：

### 第一阶段：逐节提取（从无到有）

**按章节标题逐节处理**，每次只处理当前节的截图，完成后再处理下一节。每节的工作步骤：

1. **读取当前节的所有截图**（用 Read 工具），提取图中的文字、代码、架构图等内容
2. **将截图内容转化为文档格式**：
   - 文字说明 → 中文散文或列表
   - 代码截图 → 代码块（标注语言）
   - 架构/流程图截图 → **立即转化为 ASCII 图**（不推迟到第二阶段）
3. **遇到源码路径**：按"截图中出现源码路径时的处理规则"查核并补充关键代码片段
4. **写入文档后**：删除该节的图片引用，用 Bash `rm` 删除图片文件

**分节处理的原因**：截图数量多，一次性处理容易遗漏；且后续章节内容未知，无法判断哪些是重复。

**不允许跳过图片不读**：每张图都必须读取，判断其价值后决定如何转化，不能默认忽略。

**禁止覆盖未完成章节的占位标题**：文档末尾可能存在用户预留的空白章节标题（如 `## §07 ...`、`## §08 ...`），用 Write 工具整体覆写文档时必须保留这些标题。写完正文后检查原文档末尾是否有占位标题，若有则追加到新文档末尾。

### 第二阶段：全局归纳（从有到好）

待第一阶段完成（整章所有节都已提取完毕）后执行：

1. **更新"核心机制一览"**：看完全章后重新判断哪些知识点最关键
2. **删除跨节重复内容**：同一机制在多节出现时只保留最深入的那个
3. **调整叙述连贯性**：确保各节之间有逻辑过渡，整体读起来顺畅

**两阶段不合并的原因**：第二阶段需要看到全局才能做取舍；边提取边归纳会干扰提取的完整性。

### 截图中出现源码路径时的处理规则

截图中经常会出现课程老师标注的源码文件路径（如 `staging/src/k8s.io/kubectl/pkg/cmd/create.go`）。遇到这种情况，**必须**执行以下步骤：

1. **在本地源码中找到对应文件**：路径相对于 `C:\Users\chunhsun\OneDrive - Advanced Micro Devices Inc\Desktop\platform\K8S-SourceCode\kubernetes\`
2. **读取文件，定位截图中涉及的函数或代码段**：用 Grep 精确定位，不要猜行号
3. **核实内容**：确认截图中展示的代码与本地源码一致（版本 v1.21）
4. **将关键代码片段写入文档**：
   - 只引用与知识点直接相关的核心片段，不粘贴整个函数
   - 代码块顶部注释标注文件路径（相对 `kubernetes/` 根目录），格式：`// 文件路径`
   - 代码块后用中文解释"这段代码在做什么、为什么这样设计"
   - 必要时在代码行内加 `// 注释` 标注关键逻辑，帮助理解

---

## 目录现有文档索引

| 文件 | 内容 |
|------|------|
| `第02章 创建pod时kubectl的执行流程和它的设计模式.md` | kubectl 执行流程、cobra 命令树、Visitor/Builder 设计模式 |
| `第03章 apiserver中的权限相关.md` | apiserver 启动三 server 链、Authentication/Authorization/Audit/Admission 四大权限模块源码 |
| `第04章 自定义准入控制器，完成nginx sidecar的注入.md` | MutatingAdmissionWebhook 实战：webhook server 架构、JSON Patch 注入、annotation 驱动决策、K8s CSR 证书签名 |
| `第05章 API核心服务的处理流程.md` | GenericAPIServer.New 构造模板、PostStartHook 三路合并、installAPI 四类路由、Scheme 四张 map、RESTStorage/restStorageMap 组装、Pod 创建写 etcd 全链路（Strategy/DryRun/etcd事务）、MaxInFlightLimit vs APF 限流源码 |
| `第06章 kube-scheduler 调度pod的流程.md` | cobra 启动链（main→Setup→Run）、clientset 结构、leader election 选主机制（Lease锁/tryAcquireOrRenew乐观锁）、Event 三角色（EventRecorder/EventBroadcaster/Sink）、Informer 四件套（Reflector/DeltaFIFO/Informer/Indexer）、sharedInformerFactory 按类型复用、HandleDeltas 双路径（Indexer+distribute）、SchedulingQueue 三子队列、scheduleOne Assume/Bind 分离、插件化调度框架（Filter/Score/Registry） |
| `第07章 kube-controller-manager控制管理中心的作用.md` | cobra 启动链（Config→Run）、KnownControllers+NewControllerInitializers（name→InitFunc map）、StartControllers（saToken 优先启动+遍历 map）、控制器模式（对比期望与实际驱动收敛）、ReplicaSetController（EventHandler→Queue→worker→syncReplicaSet）、slowStartBatch 渐进式扩容、getPodsToDelete 8 维缩容排序策略 |
